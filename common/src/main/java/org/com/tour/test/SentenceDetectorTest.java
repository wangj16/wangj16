package org.com.tour.test;

public class SentenceDetectorTest {
	
	/*
	 * @Test public void SentenceDetectorTest() throws IOException {
	 * SentenceDetector sentenceDetector = new SentenceDetector(); String[]
	 * sentences = sentenceDetector.
	 * detectSentence("If you have several test classes, you can combine them into a test suite. Running a test suite executes all test classes in that suite in the specified order. A test suite can also contain other test suites."
	 * ); Assert.assertTrue(sentences != null && sentences.length > 0); }
	 * 
	 * @Test public void SentencePosDetectorTest() throws IOException {
	 * SentenceDetectorImpl sentenceDetector = new SentenceDetectorImpl(); Span[]
	 * spans = sentenceDetector.
	 * detectSentencePos("If you have several test classes, you can combine them into a test suite. Running a test suite executes all test classes in that suite in the specified order. A test suite can also contain other test suites."
	 * ); Assert.assertTrue(spans != null && spans.length > 0); }
	 */

}
